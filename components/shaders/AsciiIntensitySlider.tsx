"use client"
import { Slider } from "@/components/ui/slider";
import { useAsciiIntensity } from "./ascii-intensity";


export function AsciiIntensitySlider() {
  const { intensity, setIntensity, reducedMotion } = useAsciiIntensity();

  return (
    <div className="">
     
      
    </div>
  );
}
